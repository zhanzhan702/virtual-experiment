# 2026-08-06 工作日志

## 计量小室终端编号提示面板（HMeteringTerminalGuide.vue）

- 复刻参考图的 16 列端子面板（端子 13–28），用于高压计量小室步骤 5–11 全程提示框。
- 样式：半透明背景 `rgba(171,168,158,0.85)`，内嵌 `.replica` 区背景 `#9ba89b`，端子方块 `#d8ddd4` 深字，竖向标签 `writing-mode: vertical-rl`。
- 端子语义（按用户澄清）：21=公共地(+↓)、22=多功能口高(-)、23=多功能口低(+)、24=A1、25=B1、26=-、27=公共地(A2↓)、28=B2。
- Task 11：485 接口分组改为「双端子连线汇聚到端口」样式：
  - 端子 24(center 718.75)+25(center 781.25) 汇聚到端口 center 750，端口标 485 / 接口1
  - 端子 27(center 906.25)+28(center 968.75) 汇聚到端口 center 937.5，端口标 485 / 接口2
  - 用 SVG `viewBox="0 0 1000 46"` 的 path（汇聚线）+ rect（端口框）+ text 实现。
- 用户要求：该组件**暂不接入画布**（HMeteringRoomCanvas.vue 已还原为 clean）。查看效果用 `outputs/terminal-guide-preview.html` 独立预览页（已与 vue 同步）。
- 验证：Prettier 格式化通过；`@vue/compiler-sfc` 编译检查通过（template + script 解析无误）。
- 注：因组件未被 import，整体 `vite build` 不会编译它；用 SFC 编译检查替代验证。
